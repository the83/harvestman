import { moduleForComponent, test } from 'ember-qunit';

moduleForComponent('hm-post-form', 'Integration | Component | hm post form', {
  integration: true
});

test('it renders', function(assert) {
  assert.ok(true);
});
