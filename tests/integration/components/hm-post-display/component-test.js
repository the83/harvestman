import { moduleForComponent, test } from 'ember-qunit';

moduleForComponent('hm-post-display', 'Integration | Component | hm post display', {
  integration: true
});

test('it renders', function(assert) {
  assert.ok(true);
});
